#include <stdio.h>
#include <stdlib.h>

#define MAX 1000000

void print_arr(int *arr, int i, int j) {
    while (i <= j) {
        printf("%d ", arr[i]);
        i++;
    }
    printf("\n");
}

int main(int argc, char *argv[]) {
    // int n = atoi(argv[1]);
    // int arr[MAX];

    // TODO: implement insertion sort here

    return 0;
}